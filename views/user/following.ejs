<% include ../partials/header %>
        <legend>Users <%=user.name%> follows</legend>
        
        <% users.forEach(function(following)
        { %>
            <div class="row text-left">
                <div class="col-lg-5 col-md-offset-4 col-md-6 col-sm-12 col-xs-12 text-center">
                <a href="/users/<%= following._id %>">
                <div class="searchResultImgContainer center-block">
                <img class="" src="<%=following.image%>">
            </div>
            <div class="searchResult text-left">
                <p class="lead"><%=following.name%></p>
                <%
                if(following.stats.totalPicks)
                { %>
                    <p>Total picks: <%=following.stats.totalPicks%></p>
                    <p>Lifetime correct picks: <%=following.stats.correctPicks%></p>
                    <p>Lifetime correct picks against the spread: <%=following.stats.correctSpreadPicks%></p>
                    <p>Participated in <%=following.stats.totalPools%> pools</p>
                    </a>
            </div>
            </div>
            </div>
            <br>
                <%
                } 
                else
                { %>
                        <p><%=following.username%> has not made any picks.</p>
                        </a>
                   </div>
               </div>
           </div>
                   <br>
                <%
                } %>
                  
 <!-- end row -->
          <legend></legend>
    <%  }) %>

        
<% include ../partials/footer %>